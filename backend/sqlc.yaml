version: "2"
sql:
  - engine: "postgresql"
    queries: "database/query.sql"
    schema: "migration"
    gen:
      go:
        package: "repository"
        out: "repository"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
